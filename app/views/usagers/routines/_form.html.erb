<%= form_for(@usagers_routine) do |f| %>
  <% if @usagers_routine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usagers_routine.errors.count, "error") %> prohibited this usagers_routine from being saved:</h2>

      <ul>
      <% @usagers_routine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br />
    <%= f.number_field :owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

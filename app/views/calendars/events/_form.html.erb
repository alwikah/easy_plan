<%= form_for(usagers_path(@calendars_event)) do |f| %>
  <% if @calendars_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendars_event.errors.count, "error") %> prohibited this calendars_event from being saved:</h2>

      <ul>
      <% @calendars_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.time_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.time_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br />
    <%= f.number_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
